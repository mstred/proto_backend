<div class="panel panel-default">
    <div class="panel-body">
<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-md-8">
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description,{class: 'form-control'} %>
  </div>
  <div class="field">
    <%= f.label :customer_id %><br>
    <%= f.collection_select :customer_id, Customer.all, :id, :name, {prompt: true},{class: 'form-control'} %>
  </div>
  <div class="field">
    <%= f.label :technician_id %><br>
    <%= f.collection_select :technician_id, Technician.all, :id, :name, {prompt: true},{class: 'form-control'} %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.number_field :status , {class: 'form-control'} %>
  </div>
  <br>
  <div class="actions">
    <div align="right">
      <%= submit_tag "Create" , class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>
</div>
</div>
</div>
